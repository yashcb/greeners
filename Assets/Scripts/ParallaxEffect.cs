using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParallaxEffect : MonoBehaviour
{

   /* public Transform[] layers;      // Array of layers to be parallaxed
    private float[] parallaxScales; // The proportion of the movement of camera to move layers by
    public float parallaxEffect;    // How smooth parallax is going to be, set it above 0

    private Transform camTrans;     // reference to cameras transform
    private Vector3 previousCamPos; // stores position of camera in previous frame

    private void Awake()
    {
        previousCamPos = Camera.main.transform.position;
    }
   */
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
